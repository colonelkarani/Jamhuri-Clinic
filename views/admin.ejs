<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Admin Dashboard — Jamhuri Afya</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* Basic reset & variables */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root{
            --primary:#0066cc; --primary-dark:#0052a3; --accent:#00d9ff;
            --success:#10b981; --warning:#f59e0b; --danger:#ef4444;
            --bg-light:#f8fafc; --bg-white:#ffffff; --text-dark:#1e293b;
            --text-gray:#64748b; --border:#e2e8f0; --card-radius:12px;
            --shadow: 0 1px 3px rgba(0,0,0,0.05);
            --glass: rgba(255,255,255,0.6);
        }
        body{
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, "Helvetica Neue", Arial;
            background: linear-gradient(180deg, #f4f7fb 0%, var(--bg-light) 100%);
            color:var(--text-dark);
            min-height:100vh;
            -webkit-font-smoothing:antialiased;
        }

        /* Layout */
        .container { display:flex; min-height:100vh; }
        .sidebar {
            width:300px; background:linear-gradient(180deg,var(--primary),var(--primary-dark)); color:#fff;
            display:flex; flex-direction:column; transition:width .25s ease; position:relative; z-index:1000;
            box-shadow:2px 0 10px rgba(0,0,0,0.08);
        }
        .sidebar.collapsed{ width:84px; }
        .sidebar * { -webkit-tap-highlight-color: transparent; }

        .sidebar-header{ padding:18px; display:flex; align-items:center; justify-content:space-between; gap:8px; border-bottom:1px solid rgba(255,255,255,0.06);}
        .brand{ font-weight:700; font-size:16px; overflow:hidden; white-space:nowrap; }
        .sidebar.collapsed .brand { display:none; }

        .sidebar-toggle{ background:none;border:none;color:#fff;cursor:pointer;font-size:20px; width:21px;height:21px;border-radius:8px; display:flex;align-items:center;justify-content:center; }
        .sidebar-toggle:hover{ background:rgba(255,255,255,0.1); }
        .sidebar-toggle:focus{ outline:3px solid rgba(255,255,255,0.12); }

        .user-profile{ padding:18px; display:flex; align-items:center; gap:12px; border-bottom:1px solid rgba(255,255,255,0.06); }
        .user-avatar{ width:48px;height:48px;border-radius:50%; background:var(--accent); display:flex;align-items:center;justify-content:center;font-weight:700;color:var(--primary); flex-shrink:0;}
        .user-info{ overflow:hidden; }
        .sidebar.collapsed .user-info{ display:none; }
        .user-name{ font-weight:600; font-size:14px; }
        .user-email{ font-size:12px; opacity:0.8; }

        .nav-menu{ flex:1; overflow:auto; padding:12px 6px; }
        .nav-section{ margin-bottom:18px; }
        .nav-section-title{ font-size:11px; font-weight:700; text-transform:uppercase; opacity:0.6; padding:0 12px; margin-bottom:8px; letter-spacing:0.6px; }
        .sidebar.collapsed .nav-section-title{ display:none; }

        .nav-item{
            display:flex; align-items:center; gap:12px; padding:10px 12px; cursor:pointer; border-radius:8px; transition:all .18s ease;
            color:rgba(255,255,255,0.88); font-size:14px; text-align:left; background:none; border:none; width:100%;
        }
        .nav-item:hover{ background: rgba(255,255,255,0.06); color:#fff; transform:translateX(2px); }
        .nav-item.active{ background:var(--accent); color:var(--primary); font-weight:600; box-shadow: 0 6px 14px rgba(0,102,204,0.12); }
        .nav-icon{ font-size:18px; min-width:24px; flex-shrink:0; text-align:center; }
        .nav-label{ flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
        .sidebar.collapsed .nav-label{ display:none; }

        /* Main Content */
        .main-content{ flex:1; display:flex; flex-direction:column; overflow:hidden; }
        .header{
            background:var(--bg-white); padding:18px 26px; border-bottom:1px solid var(--border); display:flex; justify-content:space-between; align-items:center; box-shadow:var(--shadow);
        }
        .header-left{ display:flex; gap:18px; align-items:center; }
        .header-title{ font-size:20px; font-weight:700; }
        .header-time{ font-size:13px; color:var(--text-gray); }
        .header-actions{ display:flex; gap:10px; align-items:center; }
        .search-input{ padding:8px 12px; border-radius:10px; border:1px solid var(--border); background:var(--bg-light); min-width:220px;}
        .search-input:focus{ outline:none; box-shadow:0 0 0 3px rgba(0,102,204,0.08); border-color:var(--primary); }

        .content{ flex:1; overflow:auto; padding:26px; }

        /* Page system */
        .page{ display:none; animation:fadeIn .18s ease both; }
        .page.active{ display:block; }
        @keyframes fadeIn{ from{ opacity:0; transform: translateY(6px);} to{opacity:1; transform:none;} }

        /* Greeting */
        .greeting-section{ background:var(--bg-white); padding:26px; border-radius:var(--card-radius); margin-bottom:22px; box-shadow:var(--shadow); border:1px solid var(--border); display:flex; justify-content:space-between; align-items:center; gap:12px; flex-wrap:wrap; }
        .greeting-text{ font-size:22px; font-weight:700; }
        .greeting-date{ color:var(--text-gray); font-size:13px; }

        /* Grid & Cards */
        .grid{ display:grid; gap:18px; margin-bottom:24px; }
        .grid-2{ grid-template-columns: repeat(auto-fit,minmax(240px,1fr)); }
        .grid-3{ grid-template-columns: repeat(auto-fit,minmax(300px,1fr)); }
        .grid-4{ grid-template-columns: repeat(auto-fit,minmax(250px,1fr)); }
        .card{ background:var(--bg-white); padding:18px; border-radius:var(--card-radius); box-shadow:var(--shadow); border:1px solid var(--border); }
        .metric-card{ padding:18px; border-radius:var(--card-radius); color:#fff; box-shadow:0 6px 18px rgba(0,0,0,0.06); cursor: pointer; transition:all .18s ease; }
        .metric-card:hover{opacity: 0.9;transform: translateY(-2px); box-shadow: 0 8px 20px rgba(0,0,0,0.15);}
        .metric-label{ font-size:12px; opacity:0.9; font-weight:700; text-transform:uppercase; letter-spacing:0.6px; }
        .metric-value{ font-size:28px; font-weight:800; margin:6px 0; }
        .metric-unit{ font-size:14px; opacity:0.9; margin-left:6px; }
        .metric-avg{ font-size:12px; opacity:1; margin-top:8px; color: #fffdfd; }

        /* Tables */
        .table-container{ background:var(--bg-white); border-radius:var(--card-radius); box-shadow:var(--shadow); border:1px solid var(--border); overflow:hidden; }
        .table-header{ background:var(--bg-light); padding:14px 18px; border-bottom:1px solid var(--border); display:flex; justify-content:space-between; align-items:center; }
        .table-title{ font-weight:700; font-size:16px; }
        .table-wrapper{ overflow-x:auto; }
        table{ width:100%; border-collapse:collapse; }
        th{ text-align:left; padding:12px 18px; font-weight:700; font-size:13px; color:var(--text-gray); text-transform:uppercase; letter-spacing:0.5px; border-bottom:2px solid var(--border); }
        td{ padding:14px 18px; border-bottom:1px solid var(--border); font-size:14px; }
        tr:hover{ background:var(--bg-light); }
        tr:last-child td{ border-bottom:none; }

        /* Badges */
        .badge{ display:inline-block; padding:4px 10px; border-radius:12px; font-size:11px; font-weight:700; text-transform:uppercase; }
        .badge-success{ background:rgba(16,185,129,0.12); color:var(--success); }
        .badge-warning{ background:rgba(245,158,11,0.12); color:var(--warning); }
        .badge-danger{ background:rgba(239,68,68,0.12); color:var(--danger); }
        .badge-info{ background:rgba(0,102,204,0.12); color:var(--primary); }

        /* Buttons */
        .btn{ padding:8px 14px; border-radius:8px; border:none; cursor:pointer; display:inline-flex; align-items:center; gap:6px; font-weight:600; font-size:13px; transition:all .18s ease; }
        .btn-primary{ background:var(--primary); color:#fff; }
        .btn-primary:hover{ background:var(--primary-dark); }
        .btn-danger{ background:var(--danger); color:#fff; }
        .btn-danger:hover{ background:#dc2626; }
        .btn-secondary{ background:var(--bg-light); border:1px solid var(--border); color:var(--text-dark); }
        .btn-secondary:hover{ background:var(--border); }
        .btn-sm{ padding:6px 10px; font-size:12px; }

        /* Empty state */
        .empty-state{ text-align:center; padding:40px 20px; color:var(--text-gray); }
        .empty-state-icon{ font-size:48px; margin-bottom:12px; opacity:0.5; }
        .empty-state-text{ font-size:14px; }

        /* Modal */
        .modal-backdrop{ position:fixed; inset:0; background:rgba(16,24,40,0.5); display:none; align-items:center; justify-content:center; z-index:4000; padding:20px; }
        .modal-backdrop.active{ display:flex; }
        .modal{ width:100%; max-width:600px; background:var(--bg-white); border-radius:14px; overflow:auto; max-height:90vh; padding:24px; border:1px solid var(--border); box-shadow:0 20px 60px rgba(0,0,0,0.3);}
        .modal-header{ display:flex; justify-content:space-between; align-items:center; gap:8px; margin-bottom:18px; }
        .modal-title{ font-weight:800; font-size:18px; }
        .modal-close{ background:none; border:none; font-size:24px; cursor:pointer; color:var(--text-gray); }
        .modal-close:hover{ color:var(--text-dark); }
        .modal-body{ margin-bottom:18px; }
        .modal-footer{ display:flex; gap:10px; justify-content:flex-end; }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar{ width:84px; }
            .sidebar .brand, .sidebar .user-info, .sidebar .nav-label, .sidebar .nav-section-title{ display:none; }
            .grid-4{ grid-template-columns: repeat(auto-fit,minmax(200px,1fr)); }
            .table-wrapper{ overflow-x:scroll; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <div class="brand">Jamhuri Afya</div>
                <button class="sidebar-toggle" id="sidebarToggle" aria-label="Toggle sidebar">☰</button>
            </div>

            <div class="user-profile">
                <div class="user-avatar">A</div>
                <div class="user-info">
                    <div class="user-name">Admin</div>
                    <div class="user-email">admin@jamhuriafya.com</div>
                </div>
            </div>

            <nav class="nav-menu">
                <div class="nav-section">
                    <div class="nav-section-title">Overview</div>
                    <button class="nav-item active" data-page="dashboard">
                        <span class="nav-icon">📊</span>
                        <span class="nav-label">Dashboard</span>
                    </button>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Management</div>
                    <button class="nav-item" data-page="users">
                        <span class="nav-icon">👥</span>
                        <span class="nav-label">Users</span>
                    </button>
                    <button class="nav-item" data-page="appointments">
                        <span class="nav-icon">📅</span>
                        <span class="nav-label">Appointments</span>
                    </button>
                    <button class="nav-item" data-page="contacts">
                        <span class="nav-icon">📧</span>
                        <span class="nav-label">Contacts</span>
                    </button>
                    <button class="nav-item" data-page="feedback">
                        <span class="nav-icon">💬</span>
                        <span class="nav-label">Feedback</span>
                    </button>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">System</div>
                    <button class="nav-item" data-page="settings">
                        <span class="nav-icon">⚙️</span>
                        <span class="nav-label">Settings</span>
                    </button>
                </div>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <header class="header">
                <div class="header-left">
                    <h1 class="header-title" id="pageTitle">Admin Dashboard</h1>
                </div>
                <div class="header-actions">
                    <input type="text" class="search-input" id="globalSearch" placeholder="Search...">
                    <button class="btn btn-secondary" onclick="location.href='/logout'">Logout</button>
                </div>
            </header>

            <div class="content">
                <!-- Dashboard Page -->
                <div class="page active" id="dashboard">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">Welcome back, Admin</div>
                            <div class="greeting-date" id="currentDate"></div>
                        </div>
                    </div>

                    <div class="grid grid-4">
                        <div class="metric-card" style="background:linear-gradient(135deg, var(--primary), var(--primary-dark));" onclick="showPage('users')">
                            <div class="metric-label">Total Users</div>
                            <div class="metric-value" id="totalUsers">-</div>
                            <div class="metric-avg">Registered accounts</div>
                        </div>
                        <div class="metric-card" style="background:linear-gradient(135deg, var(--success), #059669);" onclick="showPage('appointments')">
                            <div class="metric-label">Appointments</div>
                            <div class="metric-value" id="totalAppointments">-</div>
                            <div class="metric-avg">Scheduled & pending</div>
                        </div>
                        <div class="metric-card" style="background:linear-gradient(135deg, var(--warning), #d97706);" onclick="showPage('contacts')">
                            <div class="metric-label">Contact Messages</div>
                            <div class="metric-value" id="totalContacts">-</div>
                            <div class="metric-avg">Inquiries received</div>
                        </div>
                        <div class="metric-card" style="background:linear-gradient(135deg, var(--accent), #0891b2);" onclick="showPage('feedback')">
                            <div class="metric-label">Feedback</div>
                            <div class="metric-value" id="totalFeedback">-</div>
                            <div class="metric-avg">User feedback</div>
                        </div>
                    </div>

                    <div class="card" style="margin-top:24px;">
                        <h2 style="margin-bottom:18px; font-size:18px;">Recent Activity</h2>
                        <div id="recentActivity">
                            <div class="empty-state">
                                <div class="empty-state-icon">📋</div>
                                <div class="empty-state-text">No recent activity</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Users Page -->
                <div class="page" id="users">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">User Management</div>
                            <div class="greeting-date">Manage all registered users</div>
                        </div>
                    </div>

                    <div class="table-container">
                        <div class="table-header">
                            <div class="table-title">All Users</div>
                            <button class="btn btn-primary btn-sm" onclick="refreshUsers()">Refresh</button>
                        </div>
                        <div class="table-wrapper">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Status</th>
                                        <th>Verified</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="usersTableBody">
                                    <tr>
                                        <td colspan="5">
                                            <div class="empty-state">
                                                <div class="empty-state-icon">👥</div>
                                                <div class="empty-state-text">Loading users...</div>
                                            </div>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Appointments Page -->
                <div class="page" id="appointments">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">Appointment Management</div>
                            <div class="greeting-date">View and manage all appointments</div>
                        </div>
                    </div>

                    <div class="table-container">
                        <div class="table-header">
                            <div class="table-title">All Appointments</div>
                            <button class="btn btn-primary btn-sm" onclick="refreshAppointments()">Refresh</button>
                        </div>
                        <div class="table-wrapper">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Phone</th>
                                        <th>Date & Time</th>
                                        <th>Reason/Title</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="appointmentsTableBody">
                                    <tr>
                                        <td colspan="6">
                                            <div class="empty-state">
                                                <div class="empty-state-icon">📅</div>
                                                <div class="empty-state-text">Loading appointments...</div>
                                            </div>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Contacts Page -->
                <div class="page" id="contacts">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">Contact Messages</div>
                            <div class="greeting-date">Manage contact form submissions</div>
                        </div>
                    </div>

                    <div class="table-container">
                        <div class="table-header">
                            <div class="table-title">Contact Submissions</div>
                            <button class="btn btn-primary btn-sm" onclick="refreshContacts()">Refresh</button>
                        </div>
                        <div class="table-wrapper">
                            <table>
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Phone</th>
                                        <th>Service</th>
                                        <th>Message</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="contactsTableBody">
                                    <tr>
                                        <td colspan="6">
                                            <div class="empty-state">
                                                <div class="empty-state-icon">📧</div>
                                                <div class="empty-state-text">Loading contacts...</div>
                                            </div>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Feedback Page -->
                <div class="page" id="feedback">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">Feedback Management</div>
                            <div class="greeting-date">View user feedback</div>
                        </div>
                    </div>

                    <div class="table-container">
                        <div class="table-header">
                            <div class="table-title">All Feedback</div>
                            <button class="btn btn-primary btn-sm" onclick="refreshFeedback()">Refresh</button>
                        </div>
                        <div class="table-wrapper">
                            <table>
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Feedback</th>
                                        <th>Date</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="feedbackTableBody">
                                    <tr>
                                        <td colspan="4">
                                            <div class="empty-state">
                                                <div class="empty-state-icon">💬</div>
                                                <div class="empty-state-text">Loading feedback...</div>
                                            </div>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Settings Page -->
                <div class="page" id="settings">
                    <div class="greeting-section">
                        <div>
                            <div class="greeting-text">Settings</div>
                            <div class="greeting-date">System configuration</div>
                        </div>
                    </div>

                    <div class="card">
                        <h2 style="margin-bottom:18px; font-size:18px;">System Information</h2>
                        <div style="display:grid; gap:12px;">
                            <div style="display:flex; justify-content:space-between; padding:12px; background:var(--bg-light); border-radius:8px;">
                                <span style="font-weight:600;">Application Name</span>
                                <span>Jamhuri Afya</span>
                            </div>
                            <div style="display:flex; justify-content:space-between; padding:12px; background:var(--bg-light); border-radius:8px;">
                                <span style="font-weight:600;">Version</span>
                                <span>1.0.0</span>
                            </div>
                            <div style="display:flex; justify-content:space-between; padding:12px; background:var(--bg-light); border-radius:8px;">
                                <span style="font-weight:600;">Environment</span>
                                <span>Production</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Delete Confirmation Modal -->
    <div class="modal-backdrop" id="deleteModal">
        <div class="modal">
            <div class="modal-header">
                <div class="modal-title">Confirm Delete</div>
                <button class="modal-close" onclick="closeDeleteModal()">&times;</button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this item? This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" onclick="closeDeleteModal()">Cancel</button>
                <button class="btn btn-danger" id="confirmDeleteBtn">Delete</button>
            </div>
        </div>
    </div>

    <script>
        // Sidebar toggle
        document.getElementById('sidebarToggle').addEventListener('click', () => {
            document.getElementById('sidebar').classList.toggle('collapsed');
        });

        // Page navigation
        const pages = ['dashboard', 'users', 'appointments', 'contacts', 'feedback', 'settings'];
        const pageTitles = {
            dashboard: 'Admin Dashboard',
            users: 'User Management',
            appointments: 'Appointment Management',
            contacts: 'Contact Messages',
            feedback: 'Feedback Management',
            settings: 'Settings'
        };

        function showPage(pageId) {
            pages.forEach(page => {
                document.getElementById(page).classList.remove('active');
                document.querySelector(`[data-page="${page}"]`).classList.remove('active');
            });
            document.getElementById(pageId).classList.add('active');
            document.querySelector(`[data-page="${pageId}"]`).classList.add('active');
            document.getElementById('pageTitle').textContent = pageTitles[pageId] || 'Admin Dashboard';
            
            // Load data when page is shown
            if (pageId === 'users') refreshUsers();
            else if (pageId === 'appointments') refreshAppointments();
            else if (pageId === 'contacts') refreshContacts();
            else if (pageId === 'feedback') refreshFeedback();
            else if (pageId === 'dashboard') loadDashboard();
        }

        document.querySelectorAll('.nav-item').forEach(item => {
            item.addEventListener('click', () => {
                const pageId = item.getAttribute('data-page');
                showPage(pageId);
            });
        });

        // Set current date
        document.getElementById('currentDate').textContent = new Date().toLocaleDateString('en-US', { 
            weekday: 'long', 
            year: 'numeric', 
            month: 'long', 
            day: 'numeric' 
        });

        // Load dashboard stats
        async function loadDashboard() {
            try {
                // These would be replaced with actual API calls
                // For now, showing placeholder data
                document.getElementById('totalUsers').textContent = '0';
                document.getElementById('totalAppointments').textContent = '0';
                document.getElementById('totalContacts').textContent = '0';
                document.getElementById('totalFeedback').textContent = '0';
                
                // TODO: Replace with actual API calls:
                // const stats = await fetch('/api/admin/stats').then(r => r.json());
                // document.getElementById('totalUsers').textContent = stats.users || 0;
                // document.getElementById('totalAppointments').textContent = stats.appointments || 0;
                // document.getElementById('totalContacts').textContent = stats.contacts || 0;
                // document.getElementById('totalFeedback').textContent = stats.feedback || 0;
            } catch (error) {
                console.error('Error loading dashboard:', error);
            }
        }

        // Load users
        async function refreshUsers() {
            const tbody = document.getElementById('usersTableBody');
            tbody.innerHTML = '<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">⏳</div><div class="empty-state-text">Loading...</div></div></td></tr>';
            
            try {
                // TODO: Replace with actual API call:
                // const users = await fetch('/api/admin/users').then(r => r.json());
                // 
                // if (users.length === 0) {
                //     tbody.innerHTML = '<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">👥</div><div class="empty-state-text">No users found</div></div></td></tr>';
                //     return;
                // }
                // 
                // tbody.innerHTML = users.map(user => `
                //     <tr>
                //         <td>${user.signupName || 'N/A'}</td>
                //         <td>${user.signupEmail || 'N/A'}</td>
                //         <td><span class="badge ${user.isVerified ? 'badge-success' : 'badge-warning'}">${user.isVerified ? 'Active' : 'Pending'}</span></td>
                //         <td><span class="badge ${user.isVerified ? 'badge-success' : 'badge-warning'}">${user.isVerified ? 'Yes' : 'No'}</span></td>
                //         <td>
                //             <button class="btn btn-danger btn-sm" onclick="deleteUser('${user._id}')">Delete</button>
                //         </td>
                //     </tr>
                // `).join('');
                
                tbody.innerHTML = '<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">👥</div><div class="empty-state-text">Connect to API to load users</div></div></td></tr>';
            } catch (error) {
                console.error('Error loading users:', error);
                tbody.innerHTML = '<tr><td colspan="5"><div class="empty-state"><div class="empty-state-icon">❌</div><div class="empty-state-text">Error loading users</div></div></td></tr>';
            }
        }

        // Load appointments
        async function refreshAppointments() {
            const tbody = document.getElementById('appointmentsTableBody');
            tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">⏳</div><div class="empty-state-text">Loading...</div></div></td></tr>';
            
            try {
                // TODO: Replace with actual API call:
                // const appointments = await fetch('/api/admin/appointments').then(r => r.json());
                // 
                // if (appointments.length === 0) {
                //     tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">📅</div><div class="empty-state-text">No appointments found</div></div></td></tr>';
                //     return;
                // }
                // 
                // tbody.innerHTML = appointments.map(apt => {
                //     const date = new Date(apt.date);
                //     return `
                //         <tr>
                //             <td>${apt.name || 'N/A'}</td>
                //             <td>${apt.email || 'N/A'}</td>
                //             <td>${apt.phone || 'N/A'}</td>
                //             <td>${date.toLocaleString()}</td>
                //             <td>${apt.reason || apt.title || 'N/A'}</td>
                //             <td>
                //                 <button class="btn btn-danger btn-sm" onclick="deleteAppointment('${apt._id}')">Delete</button>
                //             </td>
                //         </tr>
                //     `;
                // }).join('');
                
                tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">📅</div><div class="empty-state-text">Connect to API to load appointments</div></div></td></tr>';
            } catch (error) {
                console.error('Error loading appointments:', error);
                tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">❌</div><div class="empty-state-text">Error loading appointments</div></div></td></tr>';
            }
        }

        // Load contacts
        async function refreshContacts() {
            const tbody = document.getElementById('contactsTableBody');
            tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">⏳</div><div class="empty-state-text">Loading...</div></div></td></tr>';
            
            try {
                // TODO: Replace with actual API call:
                // const contacts = await fetch('/api/admin/contacts').then(r => r.json());
                // 
                // if (contacts.length === 0) {
                //     tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">📧</div><div class="empty-state-text">No contacts found</div></div></td></tr>';
                //     return;
                // }
                // 
                // tbody.innerHTML = contacts.map(contact => `
                //     <tr>
                //         <td>${contact.name || 'N/A'}</td>
                //         <td>${contact.email || 'N/A'}</td>
                //         <td>${contact.phone || 'N/A'}</td>
                //         <td>${contact.service || 'N/A'}</td>
                //         <td style="max-width:300px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">${contact.message || 'N/A'}</td>
                //         <td>
                //             <button class="btn btn-danger btn-sm" onclick="deleteContact('${contact._id}')">Delete</button>
                //         </td>
                //     </tr>
                // `).join('');
                
                tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">📧</div><div class="empty-state-text">Connect to API to load contacts</div></div></td></tr>';
            } catch (error) {
                console.error('Error loading contacts:', error);
                tbody.innerHTML = '<tr><td colspan="6"><div class="empty-state"><div class="empty-state-icon">❌</div><div class="empty-state-text">Error loading contacts</div></div></td></tr>';
            }
        }

        // Load feedback
        async function refreshFeedback() {
            const tbody = document.getElementById('feedbackTableBody');
            tbody.innerHTML = '<tr><td colspan="4"><div class="empty-state"><div class="empty-state-icon">⏳</div><div class="empty-state-text">Loading...</div></div></td></tr>';
            
            try {
                // TODO: Replace with actual API call:
                // const feedback = await fetch('/api/admin/feedback').then(r => r.json());
                // 
                // if (feedback.length === 0) {
                //     tbody.innerHTML = '<tr><td colspan="4"><div class="empty-state"><div class="empty-state-icon">💬</div><div class="empty-state-text">No feedback found</div></div></td></tr>';
                //     return;
                // }
                // 
                // tbody.innerHTML = feedback.map(fb => `
                //     <tr>
                //         <td>${fb._id.substring(0, 8)}...</td>
                //         <td style="max-width:400px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">${fb.feedback || 'N/A'}</td>
                //         <td>${new Date(fb.createdAt || Date.now()).toLocaleDateString()}</td>
                //         <td>
                //             <button class="btn btn-danger btn-sm" onclick="deleteFeedback('${fb._id}')">Delete</button>
                //         </td>
                //     </tr>
                // `).join('');
                
                tbody.innerHTML = '<tr><td colspan="4"><div class="empty-state"><div class="empty-state-icon">💬</div><div class="empty-state-text">Connect to API to load feedback</div></div></td></tr>';
            } catch (error) {
                console.error('Error loading feedback:', error);
                tbody.innerHTML = '<tr><td colspan="4"><div class="empty-state"><div class="empty-state-icon">❌</div><div class="empty-state-text">Error loading feedback</div></div></td></tr>';
            }
        }

        // Delete functions (placeholder)
        let deleteItemId = null;
        let deleteItemType = null;

        function deleteUser(id) {
            deleteItemId = id;
            deleteItemType = 'user';
            document.getElementById('deleteModal').classList.add('active');
        }

        function deleteAppointment(id) {
            deleteItemId = id;
            deleteItemType = 'appointment';
            document.getElementById('deleteModal').classList.add('active');
        }

        function deleteContact(id) {
            deleteItemId = id;
            deleteItemType = 'contact';
            document.getElementById('deleteModal').classList.add('active');
        }

        function deleteFeedback(id) {
            deleteItemId = id;
            deleteItemType = 'feedback';
            document.getElementById('deleteModal').classList.add('active');
        }

        function closeDeleteModal() {
            document.getElementById('deleteModal').classList.remove('active');
            deleteItemId = null;
            deleteItemType = null;
        }

        document.getElementById('confirmDeleteBtn').addEventListener('click', async () => {
            if (!deleteItemId || !deleteItemType) return;

            try {
                // TODO: Replace with actual API calls:
                // const endpoint = `/api/admin/${deleteItemType}s/${deleteItemId}`;
                // await fetch(endpoint, { method: 'DELETE' });
                // 
                // closeDeleteModal();
                // 
                // // Refresh the current page
                // const currentPage = document.querySelector('.page.active').id;
                // if (currentPage === 'users') refreshUsers();
                // else if (currentPage === 'appointments') refreshAppointments();
                // else if (currentPage === 'contacts') refreshContacts();
                // else if (currentPage === 'feedback') refreshFeedback();
                // else if (currentPage === 'dashboard') loadDashboard();
                
                alert('Delete functionality needs to be connected to your API endpoints');
                closeDeleteModal();
            } catch (error) {
                console.error('Error deleting item:', error);
                alert('Error deleting item');
            }
        });

        // Global search (placeholder)
        document.getElementById('globalSearch').addEventListener('input', (e) => {
            const query = e.target.value.toLowerCase();
            // TODO: Implement search functionality
            console.log('Searching for:', query);
        });

        // Load dashboard on page load
        loadDashboard();
    </script>
</body>
</html>



